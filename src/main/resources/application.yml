server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/phamminhtrang11/microservice-config-repo
          default-label: main
  application:
    name: config-server
